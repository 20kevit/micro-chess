# ---> Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
backend/microchess.db
backend/.env
# Shared Lichess position source (optional local data, never committed).
puzzles.db
backend/puzzles.db
# ---> Node
node_modules/
frontend/.env
# NOTE: frontend/dist/ is intentionally COMMITTED: it is the prebuilt
# production artifact for git-push deployment (no server-side build on
# the current host). Rebuild with `npm ci && npm run build`
# (VITE_API_BASE_URL empty for same-origin) before committing.
# ---> Misc
.opencode/
*.log
.DS_Store
