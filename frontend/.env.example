VITE_API_BASE_URL=http://localhost:8000
# Web Push VAPID public key (matches backend VAPID_PUBLIC_KEY). Empty by
# default: push controls stay hidden until the deploy provides a key.
# VITE_VAPID_PUBLIC_KEY=
