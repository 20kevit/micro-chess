import { BlindfoldCalculationPlay } from "../components/exercise/BlindfoldCalculationPlay";

// Blindfold Calculation gameplay page (Mate in 1). No board is shown at any
// point: the Persian position description is the whole interface.
export function BlindfoldCalculationPage() {
  return <BlindfoldCalculationPlay />;
}
