import { OpeningTrapsPlay } from "../components/exercise/OpeningTrapsPlay";

// Opening Traps Blindfold gameplay page. No board is shown at any point:
// the opening context plus the Persian position description is the whole
// interface.
export function OpeningTrapsPage() {
  return <OpeningTrapsPlay />;
}
