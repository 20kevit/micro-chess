import { RuleOfTheSquarePlay } from "../components/exercise/RuleOfTheSquarePlay";

// Rule of the Square gameplay page. The board is always visible; the
// square itself is only marked after submission, never while solving.
export function RuleOfTheSquarePage() {
  return <RuleOfTheSquarePlay />;
}
