import { OpeningReconstructionPlay } from "../components/exercise/OpeningReconstructionPlay";

// Opening Move Reconstruction gameplay page. Two boards: the read-only
// target on top, the user-driven reconstruction board below.
export function OpeningMoveReconstructionPage() {
  return <OpeningReconstructionPlay />;
}
