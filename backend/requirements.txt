# MicroChess backend production dependencies for cPanel.
#
# SOURCE OF TRUTH: backend/pyproject.toml ([project] dependencies).
# This file is a generated mirror, not an independent list: cPanel's
# Python Application Manager installs from a requirements file, while
# local development installs from pyproject metadata (`pip install -e
# ".[test]"`). After changing pyproject dependencies, update this file
# to match and redeploy.
#
# Floors mirror pyproject exactly (no independent pins). All packages
# are pure-Python or ship manylinux wheels for Python 3.12, so no
# compiler toolchain is needed on the shared host.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.0
pydantic-settings>=2.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-chess>=1.9
python-multipart>=0.0.9
email-validator>=2.0
a2wsgi>=1.10
